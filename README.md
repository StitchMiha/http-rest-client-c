## Dimovska Mihaela 323CB

Starting with the schelet of lab9, which already handles socket communication, building and sending http requests I start of my project with that base.

login_admin() - Now the first part of my homework begins and that is the implementation of reading login_admin + username =  + pass = from stdin, building a POST request with the JSON body sending it over with socket, parsing and storing the Set-Cookie, and printing the correct SUCCESS or ERROR message with explanations.
I firstly start by defining a struct called ClientState which will track session state throughout multiple commands that includes the session cookie, JWT token, and whether or not the user is already logged in or not. Basically to manage the state of clients across the commands. I define this structure in my client.h. I also create an initialization function for ClientState. After looking over and understanding the functions that come for the Parson JSON library, I included them in my parson.c and parson.h files.
I moved on to defining my compute_post_json_request_json using the same style of function syntax provided in the labs. This function builds an HTTP POST request with: header;  content-type(json) ; content-length; cookies; and the JSON body payload. Initially, I used a single session_cookie field in my ClientState struct to store the session information after login. However, as I progressed through the implementation, I realized that separating the logic for admin and normal users would make the session handling more better because admins and users have different roles, and reusing the same cookie field would have led to confusion during authentication and privilege validation. To solve this, I replaced the session_cookie field with two separate fields: admin_cookie for admin sessions and user_cookie for normal users. This way the state is preserved clearly across commands.
And finally my implementation  for the very first command login_admin() in commands.c that encapsulates the entire logic of the first command.
So when a server replies with a HTTP/1.1 200 OK is also sends back a session cookie in the HTTP response header. After i just extract the cookie string and after I save it in the state->admin_cookie.

add_user() - This function should add a new normal user. The specifications state that it must be called only after login as abn admin. It sends a POST request to /api/v1/tema/admin/users with the same type of payload as the first command and same fields. However here you need to firstly demonstate that you have a role as an admin and that the user is apart of the current admin. I also need to treat the errors when there is no admin and when the information or data are incomplete or wrong. So noticing that I am building a json body for two times a define a helper function in utils.c that will create the json body for me and make my code more modular and seeing as how i need to treat more error codes this time I also define a helper function get_response_status which returns the correct value and implemented it in my logic. I also needed a cleanup function to free the memory so i define also cleanup_request_resources
The full logic of my function first starts with with checking whether the admin is already logged in using the logged_in_flag and if that fails an error is printed. Then as before I need the user to enter username and password using fgets. Using the previously specified build_json_body  which dynamically created a JSON object using parson library, it returns then the serialized string that needs tom be send as the request body. The request is being send again witht he custom compute_post_json_request_json. I then parse the  HTTP status code using another helper function get_response_status which extracts the status code integer from the response line and then depending and what that response code is I display messages.

get_users() - The next command i needed to implement was the get_users command, which gets this endpoint GET /api/v1/tema/admin/users, and I also need to include the session cookie to prove admin privileges. This function should receive a JSON array from the server that contains a list of user entries, and must handle the missing admin privilages (403 Forbidden).
So I firstly checked if an admin is logged in using my state->logged_in_flag, and I build a GET request with the session cookie reusing the compute_get_request I had taken from the lab schelet and add the cookie from state->session_cookie. Then I open the socket connection and send request. After I just read the response and correctly parse it, use another helper function I have implemented called print_user_list_from_response and finally clean it up again.

delete_user() - After I had to implement the command delete_user which takes the endpoint DELETE /api/v1/tema/admin/users/:username and it needs to delete a user. (and the username is directly provided by the admin). If admin is not logged in we print out an error message. The session cookie is attached to authenticate the request and then the URL is dynamically made using snprintf and URL_LEN which I have defined as 200 maximum. This part is very important because it tells the server exactly which user to delete. Then the response is handled using get_response_status() to deal with the codes respectively as in the previous implementations. In the completion of this exercise i defined a helper function called compute_delete_request exactly as in the labs for GET and POST but here it handles DELETE.

logout_admin() - Moving on to logout_admin. This command handles the logout functionality for an admin user. It sends a GET request to the endpoint /api/v1/tema/admin/logout and this operation requires that the admin is already logged in and if he is not an error message gets printed out. The session cookie is attached to the request using the cookies[] array, just like in the previous authenticated requests. And after sending the request the function checks the status code for the response using get_response_status()  which I already explained.

login_user() - Because in my implementation thus far I only kept track of whether an admin is logged in or not, I went back and added a user_logged_in_flag in my ClientState struct. This new flag helps me track if a normal user is currently logged in, allowing me to support simultaneous admin and user sessions more clearly and safely. I also updated init_client_state() to initialize this new flag to 0. The same as my login_admin logic: the client asks for three inputs. Once the request is built and sent to the server using compute_post_json_request_json(), i handle the server response and extract the user session cookie (which i have separated from the admin cookie) and store it in state->user_cookie. The reasoning behind my separation was that admin and users have different roles and different permissions and having only a single session flag or cookie would have created confusion as well as to make sure that they have their role-specific access control.

get_access() - In order to complete the get_access task, I needed a field which will store my JWT token, so I went back again and updated my ClientState structure once more to add char jwt_token[] and well as update my init_client_state(). When writing my get_access function the first thing I needed to make sure was that a user was already logged in. I then built a GET request to the /api/v1/tema/library/access. And if the server responded with status 200 ok, I used another helper function extract_token_from_response to parse the JSON body and extract the JWT token, which I stored in state->jwt_token, which will be necessary in the future when future requests need authorization.


get_movies() - For the get_movies command, I firstly check if this client has the library access by checking if a valid JWT token exists in the state->jwk-token. I then build a GET request, attaching the token in the Authorization: Bearer TOKEN header. Since my original compute_get_request() function didnt support custom headers, I implemented a new helper function called compute_get_request2() to allow adding headers like Authorization. If the server responds with status code 200 OK, I use a helper function print_movies() to display the list of movies (showing each movie's id and title) from the returned JSON array.

get_movie() - For the get_movie command, I followed the instructions that say to send a GET request to /api/v1/tema/library/movies/:id, using the JWT token obtained earlier with get_access. First, I read the movie ID using my read_input helper, then dynamically built the URL by appending the ID to the end of it. Since authentication is required, I also built the Authorization: Bearer token header using the token stored in ClientState. I used my compute_get_request2() helper to build the request because the original compute_get_request() didn’t support custom headers. After sending the request with send_and_receive_request(), if the response was successful, I printed the movie details using a separate helper,print_movie_details(), which extracts and formats the title, year, description, and rating from the JSON correctly.

add_movie() - For the add_movie command, I first check if the user is logged in and has access to the library using the JWT token. If both checks pass, I read the movie info from the user using read_input for title, year, description, and rating. Since year and rating are expected as numbers in the JSON payload, I convert them from string to int and double. I also added a check to make sure the rating stays between 0 and 9.9, since I had an error earlier where the rating wasn't being validated properly. After that, I used my build_movie_json() helper to create the JSON payload, and built the Authorization header with the token. Then I built the request using compute_post_json_request2() (which supports custom headers), sent it with send_and_receive_request(), and handled the possible server responses like 403 (no access) or 400 (invalid data). If everything goes fine, I print a success message.

update_movie() - To implement the update_movie command, I first made sure the user is both logged in and has library access. Then I read all the necessary movie fields from the user — the movie ID, title, year, description, and rating — using my read_input helper to keep input handling clean. I used a separate build_movie_json function to construct the JSON body, and added the JWT token in the Authorization header like it was required. The actual PUT request was built using compute_put_json_request, and sent using my send_and_receive_request function. If the response was successful I printed a success message and an error message if it wasnt successful.

delete_movie() - To implement the delete_movie command, I first made sure the user is logged in and has access to the library by checking the JWT token. Then I asked for the movie ID using read_input and built the endpoint URL dynamically using snprintf. Just like with other authenticated endpoints, I added the Authorization: Bearer token header. I used compute_delete_request2 to build the DELETE request with custom headers, and send_and_receive_request to send it. If the status code in the response was Ok, I print a success message and if not I treat the error codes.

get_collections() - For the get_collections command, I first make sure the user is logged in and has a valid JWT token using is_user_logged_in and does_have_access. Since this endpoint requires authentication, I build an Authorization: Bearer token header and pass it to my custom request function compute_get_request2, which supports headers as I had done for all the previous commands. After sending the request to /api/v1/tema/library/collections, I check the response code. If it’s successful, I call the helper function print_collections, which extracts the JSON array of collections from the response, iterates through each object, and prints out the collection ID and title in a correct wau.

get_collection() - To implement the get_collection command, I first make sure the user is logged in and has a valid JWT token stored in state->jwt_token. Then, a user enter the collection ID using read_input, and  i build the URL dynamically by appending that ID to the endpoint path. Since authentication is required, I create an Authorization: Bearer token header and use it in a custom request function, compute_get_request2, which allows sending custom headers. After getting the server’s response, I check the status code and if it’s 200, I call a helper function print_collection_details which handles parsing the JSON response and printing the collection’s title, owner, and list of movies.  

add_collection() - To implement the add_collection command, I started by checking if the user is logged in and has access to the library, using the session flags and JWTtoken. After that the user needed to enter the collection title and i used my helper function build_collection_body() to construct a JSON obhect with the title field. I then send a POST request to /api/v1/tema/library/collections, including the JWT in the Authorization header. When the collection was created with success the user needed to specify how manyb movies to add. To get the correct ID of the newly created collection, I made a new helper function get_last_collection_id() which send a GET request to /api/v1/tema/library/collections, retrieves the full list, and extracts the ID of the most recently added collection. After for each of the movies, i asked the user for its ID, made a JSON object with only the id field using json_object_set_number and then sent a POST request to /collections/:id/movies to associate the movie with the collection. Then I just free everything that was previously allocated with my cleanup_request_resources().

delete_collection() - I follow all the previous steps, the only thing I change is that I use my helper function compute_delete_request2()which also includes headers to build the HTTP DELETE request and send it using send_and_receive_request() and finally handle the status code received.

add_movie_to_collection() - To implement the add_movie_to_collection command, I followed the usual steps, and after reading the collection ID and movie ID from the user, I needed to send a POST request to the /api/v1/tema/library/collections/:id/movies endpoint. For the request body, I created a helper function called build_id_body(). Then, using the existing compute_post_json_request2() function, I built the request, including the Authorization header with the JWT token.

delete_movie_from_collection() - Exact same logic, as before,after I build the Authorization header with the JWT token and send the DELETE request using compute_delete_request2() and send_and_receive_request() and print out success or error depending on the status code.

All of the helper function used are explained even better in detail with the comments in utils.c